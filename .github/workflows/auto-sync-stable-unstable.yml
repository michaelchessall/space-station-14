name: Sync Stable To Unstable [Automatic]

on:
  workflow_dispatch:
  schedule:
    # To run weekly
    # - cron: '0 0 * * 0' Disabling auto run until testing is complete

jobs:
  sync-unstable:
    uses: ./.github/workflows/sync-stable-base.yml
    with:
      target_branch: staging-unstable